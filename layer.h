// #ifndef LAYER_H
// #define LAYER_H

// typedef void (*Activation)(struct Node *); 
// typedef void (*Activate)(struct Layer *);

// struct Layer {
//   Activation activation;
//   Activate activate; 
//   struct Node *nodes;  
//   int cnt;
// };

// struct Layer *new_layer(Activation activation, int cnt);
// void dot(struct Layer *pre, struct Layer *next);
// void free_layer(struct Layer *);
// #endif